import { defineConfig } from 'astro/config';

// Quando você publicar, troque a linha abaixo pelo endereço real do site,
// por exemplo: site: 'https://seucaderno.netlify.app'
export default defineConfig({
  // site: 'https://exemplo.netlify.app',
});
